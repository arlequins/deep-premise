# Type0 archive

Type0 is the existing v0.7.0 habit-world prototype, retained after the player requested a fresh Type1 design on 2026-09-10.

- Explicit scene: `game/type0.tscn` (uses the existing `HabitMain.cs`).
- The default scene remains unchanged during the Type1 concept review.
- Source checkpoint: `artifacts/checkpoints/type0-source-20260910-191252.zip`.
- The checkpoint includes tracked and nonignored untracked source files as they existed before the Type0 scene alias was added. It excludes ignored tools, build artifacts, and runtime saves.
- Existing saves and playtest recordings remain in their original locations. No reset or migration was performed.

The scene alias shares source with the current prototype; the ZIP is the immutable source snapshot. Future Type1 implementation must use separate gameplay source, scene, and save identity.

Type1 is currently a concept review, not an implemented or playtested successor. See `TYPE1_CONCEPT_REVIEW.md` for the discussion outcome.
